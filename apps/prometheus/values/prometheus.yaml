kubeStateMetrics:
  enabled: false
nodeExporter:
  enabled: false
pushgateway:
  enabled: false

alertmanager:
  updateStrategy:
    type: Recreate

server:
  image:
    tag: v2.31.0

  updateStrategy:
    type: Recreate

  extraArgs:
    storage.tsdb.retention.size: 540GB

  persistentVolume:
    storageClass: ceph-block
    size: 600Gi
