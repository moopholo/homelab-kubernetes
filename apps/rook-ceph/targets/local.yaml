rook-ceph:
  kubernetes-version: "1.22"
  csi:
    kubelet-root-dir: /var/lib/kubelet
  storage:
    device-filter: "^sda"
  dashboard:
    domain: rook-ceph.homelab.monetact.net
    port: 8080
  obj-store:
    gateway:
      port: 6080
